name: website
description: "Habit tracker"
publish_to: 'none'
version: 1.0.1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.1
  http: ^1.2.2
  cupertino_icons: ^1.0.8
  fetch_client: ^1.1.2
  pair: ^0.1.2
  bcrypt: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - web/icons/.
    - web/icons/navigations/.

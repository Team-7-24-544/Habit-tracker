class HabitService {
  // Метод для отметки привычки как выполненной
  Future<void> markHabitAsCompleted(String habitId, bool completed) async {
    // Здесь будет реализована логика сохранения состояния привычки
    // Например: обновление в базе данных, отправка на сервер и т.д.
  }
}